//#include<dim-handler.hpp>
/*dim_type getMaxDim(dim_type * dims, int ndims) {
  dim_type indexDiff;
  if(vrZeroBasedIndex) {
    indexDiff = 0;
  }else{
    indexDiff = 1;
  }
  dim_type prod = 1;
  dim_type maxDim=0;
  for(int i = 0; i < ndims; i++) {
    maxDim += (dims[i]-indexDiff)*prod;
    prod *= dims[i];
  }
  return maxDim;
}
*/
