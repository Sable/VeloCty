/*template<class ArrayType>
ArrayType floor( VrArrayPtrF64 arr) {
	dim_type numel = getNumElem(arr.dims,arr.ndims);
	ArrayType out(arr.ndims,arr.dims);
	for(int i = 0; i < numel; i++) {
		out.data[i] = floor(arr.data[i]);	
	}
	return out;
}
*/
